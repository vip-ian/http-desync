ServerName backend
ErrorLog /proc/self/fd/2
LogLevel warn
LogFormat "LAB_BACKEND peer=%{c}a port=%{remote}p pid=%P keepalive=%k status=%>s connection=%X request=\"%r\"" desync_lab
CustomLog /proc/self/fd/1 desync_lab
